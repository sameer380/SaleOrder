import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
	colors: {
		
		light: {
			
			bg: "#FFF",
			
			text: "#333333",
		},
		dark: {
			
			bg: "#1A202C",
			
			text: "#FFFFFF",
		},
	},
});

export default theme;
